# AreWarin Portal V12 — Public Registration Subweb

V12 เพิ่มเว็บไซต์รับสมัครนักเรียนสาธารณะ โดยเชื่อมกับฐานข้อมูล Supabase และระบบหลักชุดเดียวกัน

## โครงสร้างการทำงาน

```text
ระบบหลักเพิ่ม/แก้คอร์ส ติวเตอร์ แพ็กเกจ และรอบเรียน
                         ↓
              /register/ โหลดข้อมูลจริง
                         ↓
        นักเรียนเลือกคอร์ส ครู และเวลาที่ว่าง
                         ↓
          ส่งใบสมัครสถานะรอแอดมินตรวจ
                         ↓
            Dashboard แสดงใบสมัครใหม่
                         ↓
                   แอดมินอนุมัติ
                         ↓
Students + Enrollment + Schedule + Tutor Assignment + Payment Plan
```

ใบสมัครยังไม่สร้างนักเรียนที่กำลังเรียนทันที จนกว่าแอดมินจะอนุมัติ ช่วยป้องกันข้อมูลทดสอบ ข้อมูลซ้ำ และการเลือกคอร์สผิด

## ฟังก์ชันในหน้า `/register/`

- นักเรียนใหม่
- นักเรียนเดิม/ต่อคอร์สด้วย Student ID เดิม
- เรียนเดี่ยวและเรียนกลุ่ม
- เลือกหลายคอร์สในใบสมัครเดียว
- ดึงคอร์สที่อนุมัติและเปิดรับจากระบบหลัก
- ดึงแพ็กเกจ ราคา จำนวนชั่วโมง และรายละเอียดวิชา
- ดึงติวเตอร์ที่ถูกผูกกับคอร์สนั้นจริง
- ดึงรอบเรียนและจำนวนที่นั่งคงเหลือ
- ป้องกันเลือกรอบที่เวลาชนกัน
- นักเรียนเดิมไม่สามารถเลือกรอบที่ชนกับตารางเดิม
- กรอกข้อมูลนักเรียน ผู้ปกครอง โรงเรียน และเป้าหมาย
- รองรับชำระเต็ม มัดจำ แบ่งชำระ และชำระภายหลัง
- ตรวจโค้ดส่วนลดจากฐานข้อมูล
- สร้าง QR PromptPay และนับถอยหลัง 15 นาที
- อัปโหลดสลิปเข้า Storage แบบ Private
- เช็กสถานะด้วยเบอร์โทรและเลขใบสมัคร
- ส่งคำขอจ้างวิทยากรเข้าสู่ Dashboard
- ใช้ Toast และข้อความในแบบฟอร์ม ไม่มี SweetAlert
- รองรับมือถือ iPad และ Desktop

## ฟังก์ชันในระบบหลัก

หลังติดตั้ง `index.html` V12 จะมีปุ่ม **ใบสมัคร V12** สำหรับแอดมิน พร้อมจำนวนใบสมัครที่รอตรวจ

เมนูภายในประกอบด้วย:

1. ใบสมัครและสลิป
2. ตั้งค่าหน้ารับสมัครของคอร์ส
3. รอบเรียนและจำนวนที่นั่ง
4. แพ็กเกจ/ราคา/ชั่วโมง
5. โค้ดส่วนลด
6. คำขอจ้างวิทยากร

เมื่อแอดมินอนุมัติ ระบบจะ:

- ค้นหานักเรียนเดิมจากเบอร์โทรเพื่อป้องกันข้อมูลซ้ำ
- สร้างหรืออัปเดต Student ID
- ลงทะเบียนนักเรียนเข้าคอร์ส
- ผูกติวเตอร์ตามแต่ละคอร์ส
- สร้างตารางเรียนจริง
- บันทึกจำนวนที่นั่งกรณีเรียนกลุ่ม
- สร้างสถานะชำระและแผนแบ่งชำระ
- ส่งการแจ้งเตือนไปยัง Student Portal

## การซิงก์คอร์สจากระบบหลัก

V12 เพิ่ม Trigger สำหรับคอร์สที่สร้างใหม่หลังติดตั้ง:

- สร้างแพ็กเกจมาตรฐานจากชื่อคอร์ส ชั่วโมง และราคา
- สร้างรอบมาตรฐานเมื่อคอร์สมีวันและเวลาเริ่ม–สิ้นสุด
- อัปเดตราคา ชั่วโมง และรอบมาตรฐานตามข้อมูลระบบหลัก
- แพ็กเกจหรือรอบที่แอดมินปรับเองจะไม่ถูก Trigger เขียนทับ

คอร์สจะปรากฏในหน้า Public เมื่อมีสถานะครบ:

```text
is_active = true
is_published = true
approval_status = approved
is_open_for_enrollment = true
ยังไม่เลย enrollment_deadline
```

## วิธีติดตั้ง

### 1. ตรวจ V11.1

รันไฟล์:

```text
CHECK-V12-PREREQUISITES.sql
```

ผลควรเป็น `true` ทุกช่อง หากไม่ครบให้ติดตั้ง V10, V11 และ V11.1 ตามลำดับก่อน

### 2. รัน SQL V12

เข้า:

```text
Supabase → SQL Editor → New query
```

เปิด `portal-v12-registration-subweb.sql` คัดลอกทั้งหมด วาง และกด **Run**

ผลท้ายไฟล์ควรเป็น `true`:

```text
applications_ready
application_items_ready
packages_ready
schedule_slots_ready
public_submit_ready
admin_approval_ready
private_slips_ready
```

### 3. อัปโหลดเว็บไซต์

```text
AreWarinTh/
├── index.html
├── register/
│   └── index.html
├── staff/
│   └── index.html
├── student/
│   └── index.html
└── library/
    └── index.html
```

ไฟล์ `staff`, `student` และ `library` ในแพ็กเกจเป็นสำเนาที่เข้าชุดกับ V11.1 สามารถใช้แทนไฟล์เดิมได้ แต่ส่วนที่จำเป็นต่อ V12 คือ `/index.html` และ `/register/index.html`

### 4. ตั้งคอร์สสำหรับรับสมัคร

เข้าเว็บหลัก แล้วกด **ใบสมัคร V12**

1. แท็บคอร์ส: เปิดรับสมัคร ใส่วิชา ระดับชั้น รายละเอียด และเลือกติวเตอร์
2. แท็บแพ็กเกจ: ใส่ราคา ชั่วโมง หรือเรตรายชั่วโมง
3. แท็บรอบเรียน: ใส่วัน เวลา จำนวนที่นั่ง รูปแบบ และสถานที่/ลิงก์
4. เปิด `/register/` เพื่อตรวจหน้าสาธารณะ

## ลำดับทดสอบก่อนเปิดจริง

1. สร้างคอร์สทดสอบในระบบหลัก
2. ตั้งให้ Active, Published, Approved และ Open for enrollment
3. ผูกติวเตอร์กับคอร์ส
4. สร้างแพ็กเกจและรอบเรียน
5. ส่งใบสมัครนักเรียนใหม่
6. ตรวจ Dashboard ว่ามีรายการรอตรวจ
7. เปิดสลิปแบบ Signed URL
8. กดอนุมัติ
9. ตรวจ Portal Manager ว่ามี Student ID
10. ตรวจ Enrollment และตารางเรียน
11. เข้าด้วย Tutor Hub เพื่อตรวจว่านักเรียนปรากฏกับครูที่เลือก
12. เปิด Student Portal เพื่อตรวจคอร์ส ชั่วโมง การชำระ และตาราง

## หมายเหตุ

- ไม่ต้อง Deploy Edge Function ใหม่
- ไม่ต้องเปลี่ยน Verify JWT
- Public API คำนวณราคา ส่วนลด และจำนวนที่นั่งจากฐานข้อมูล ไม่เชื่อค่าราคาที่ Browser ส่งมา
- Storage สลิปเป็น Private และแอดมินเปิดผ่าน Signed URL
- ควรทดสอบด้วยข้อมูลตัวอย่างหนึ่งชุดก่อนเปิดรับสมัครจริง
